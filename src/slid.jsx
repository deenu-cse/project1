import moviedata from './mdata'

export default [
    {
        "specificThumbnail2": "moviedata.length > 0 ? moviedata[7].thumbnail : ''"

    },
    {
        "specificThumbnail1": "moviedata.length > 0 ? moviedata[9].thumbnail : ''"
    },
    {
        "specificThumbnail3": "moviedata.length > 0 ? moviedata[4].thumbnail : ''"
    },
    {
        "specificThumbnail4": "moviedata.length > 0 ? moviedata[6].thumbnail : ''"
    },
    {
        "specificThumbnail5": "moviedata.length > 0 ? moviedata[10].thumbnail : ''"
    },
    {
        "specificThumbnail6": "moviedata.length > 0 ? moviedata[11].thumbnail : ''"
    },
    {
        "specificThumbnail7": "moviedata.length > 0 ? moviedata[23].thumbnail : ''"
    }
]